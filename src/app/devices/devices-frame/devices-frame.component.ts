import { Component } from '@angular/core';

const template = require('./devices-frame.template.html');

@Component({
    selector: 'sh-devices-frame',
    template
})
export class DevicesFrameComponent {}
