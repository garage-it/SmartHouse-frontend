import { Component } from '@angular/core';

const template = require('./device-list-with-frame.template.html');

@Component({
    selector: 'device-list-with-frame',
    template,
})
export class DeviceListWithFrameComponent {}
