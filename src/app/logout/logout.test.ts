import { LogoutComponent } from './logout.component';

describe('Logout', () => {
    describe('#constructor', () => {
        it('should be defined', () => {
            expect(LogoutComponent).toBeDefined();
        });
    });
});
